# Request

Implement and run the MNIST experiment in `TASK.md`. Follow `AGENTS.md` and the project Skill.

Make both figures legible when projected. In `REPORT.md`, record accuracy and macro F1, then describe the most common digit confusions in the final predictions.
