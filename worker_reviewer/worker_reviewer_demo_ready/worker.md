# Worker

Build the small Iris/KNN project described in `workflow.md`. Keep the implementation easy to read and separate the training logic from the command-line entry point so it can be tested.

Add tests for the main data and prediction flow, run the project, and save a compact result with the accuracy and confusion matrix. Do not hide data problems or failed commands.

When finished, report the files changed, commands run, main result, and anything that still looks uncertain. Leave the workflow documents unchanged and do not approve your own work.

